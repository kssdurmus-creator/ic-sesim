plugins {
    id "com.android.application"
    id "kotlin-android"
    id "flutter"
}

android {
    namespace "com.example.ic_sesim"
    compileSdk 34

    defaultConfig {
        applicationId "com.example.ic_sesim"
        minSdk 21
        targetSdk 34
        versionCode 1
        versionName "1.0.0"
    }

    buildTypes {
        release {
            signingConfig signingConfigs.debug
        }
    }
}

flutter {
    source "../.."
}
